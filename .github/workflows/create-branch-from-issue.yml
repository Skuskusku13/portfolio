name: Create Branch from Issue

on:
  issues:
    types: [opened, assigned]

jobs:
  call-create-branch:
    uses: skuskusku13/devops/.github/workflows/reusable-create-branch.yml@main
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
